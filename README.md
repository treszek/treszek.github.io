# treszek.github.io

Devlog & notes. Built with [Eleventy](https://www.11ty.dev/), deployed to GitHub Pages via Actions.

Boards are defined in `src/_data/boards.js`.

```bash
npm install
npm run dev     # local preview at :8080
npm run build   # output to _site/
```
